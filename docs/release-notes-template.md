# Cloudflare PowerShell `<version>`

> Candidate/release status: `<non-published candidate | preview | stable>`
> Source commit: `<40-character SHA>`

## Highlights

Describe user-visible changes from the reviewed semantic artifacts.

## Compatibility and public surface

- P2.4 compatibility result: `<summary and receipt>`
- Public admission changes: `<none or exact admitted cmdlets>`
- Runtime changes: `<summary>`
- Breaking changes: `<none or exact migration note>`

## Validation and provenance

- Schema revision/source version/SHA-256: `<values from candidate manifest>`
- Compatibility and coverage gates: `<receipt hashes/status>`
- Package and candidate ZIP hashes: `<values>`
- Remote CI run and artifact digests: `<URL/values, or explicitly unverified>`

## Supported environment

Use [`support-matrix.md`](./support-matrix.md) and list only environments with
current evidence.

## Known limitations

List unresolved runtime, real-account, manual UX, package, Gallery, signing, or
platform limitations. Do not imply that mock or local evidence closes them.

## Security

Summarize the reviewed security changes, supported versions, reporting channel,
and any deferred signing/supply-chain decision. Never include a token or secret.

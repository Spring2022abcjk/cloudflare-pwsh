# P4.2 Progress

## Branch and baseline

- Worktree: `E:\桌面\edit\cloudflare-pwsh-p42-release-security`
- Branch: `feature/p42-release-security`
- Base: `main` at `97f09dc3038fad95d32bca84dd5fbd0e28611cad`
- Current version: `0.1.0` (unchanged)
- Scope remains non-publishing release/security groundwork.

## Implemented

- Full SHA pinning with readable version comments for all external Actions.
- `persist-credentials: false`, read-only workflow permissions, concurrency,
  timeouts, bounded artifact retention, and explicit `pull_request`/dispatch
  behavior.
- Exact .NET SDK `10.0.400`, `global.json`, NuGet lock files, and locked restore.
- SHA-256 validation for the fallback PowerShell 7.6.6 MSI.
- `tests/P42Security.Tests.ps1` for workflow/secret/pinning/lock checks.
- `tools/Test-P42ReleaseMetadata.ps1` for version, host, dependency, schema-pin,
  and optional candidate metadata checks.
- `tools/Test-P42PackageProvenance.ps1` for candidate source/schema/gate/report/
  module/ZIP hash binding and secret-shaped-value checks.
- `tools/Test-P42GalleryPreflight.ps1` for read-only manifest/metadata validation;
  it reports the current candidate as `NotReady` without accessing a Gallery or
  API key.
- Dependabot maintenance configuration for Actions and NuGet review PRs.
- `SECURITY.md`, P4.2 plan/security review/release policy/checklist/support and
  release-note process documentation.

## Evidence in this worktree

- P4.2 static security test: **PASS**.
- P4.2 release metadata test: **PASS** with the pinned schema checkout; release
  mode also requires a clean worktree so a dirty local candidate cannot be
  presented as commit-complete provenance.
- Full locked restore and Release build: **PASS**, 0 warnings and 0 errors.
- P3.4 CI gate suite, package-twice byte check, package smoke, provenance
  validation, P2.4 regression, and schema-update isolated validation: **PASS**
  after restoring the pinned schema.
- Remote CI: **PENDING**. The public repository and `origin` now exist, and
  `feature/p42-release-security` plus the unchanged `main` baseline are pushed;
  the first non-publish remote run is still required.
- Gallery publication: **NOT ATTEMPTED**; project/tags/release-note metadata is
  configured, but read-only Gallery preflight remains **NotReady** because no
  explicit LICENSE or `LicenseUri` exists.
- Private vulnerability reporting: **READY** through GitHub Security Advisories
  private reporting for the public repository.
- Signing: **EXPLICITLY DEFERRED** for the first public release, with documented
  rationale and revisit triggers.
- Real-account, manual UX, and release approval: **separate/pending**.

## Current decision

P4.2 remains **Partial**, not ready for P4.3. The remaining release/security
blockers are remote CI evidence and an explicit license decision for Gallery
preflight. Local P3.4/P4.2 controls and repository security settings are
verified, but they do not replace remote or manual acceptance boundaries.

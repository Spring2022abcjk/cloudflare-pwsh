# ABC integration summary

Date: 2026-09-22. Scope: integrate A, then B, then C; no P4.3 or publication.

## Source history

| Item | Commit |
| --- | --- |
| Main base | `97f09dc3038fad95d32bca84dd5fbd0e28611cad` |
| A approved HEAD | `e562b595ae17d82da58628ccd95b18b1ae2e1a82` |
| A merge | `c2625fe2bedcda16c2c02dd4a9e1ace96cda4907` |
| B approved HEAD | `2f3f167f13f8d748657ad1e4c8b7836b36a11728` |
| B rebased HEAD | `73ca5253772c927f0c799036fa9c0ee72747420c` |
| B merge | `2b7d6d42872629f427ab8705e4f6be02cc32c852` |
| C approved HEAD | `8599509863587bba0e084804da1411c67dd9a982` |
| C rebased HEAD | `1037a9fa98af6e00fa5883919879405137003e03` |
| C merge | `00f63413afa196be93172be950e7d14c08eaafc6` |

Final validation targets the documentation commit containing this summary,
resolved with `git log -1 --format=%H -- docs/P4.2-abc-integration-summary.md`.
The exact final HEAD, local/clean-checkout outcomes, candidate SHA-256,
coverage and compatibility SHA-256, and new remote run result are recorded
in the validation receipt attached to that commit using
`git notes --ref=abc-integration show <final-HEAD>` and in the delivered
external integration receipt. Attaching evidence as a Git note does not
change the source HEAD after the candidate has been built. A missing receipt
means final gates remain unverified; this document alone is not a PASS.

## Validation output hygiene

Before any merge, all untracked files were enumerated with both
`git status --short` and `git ls-files --others --exclude-standard`.
All 12 roots were classified A (disposable outputs of existing tooling);
no handwritten or unknown-source B/C content was found.

A roots: `artifacts/p34-candidate-current/`, `artifacts/p34-candidate-e562b59/`,
`artifacts/p34-candidate-final/`, and `artifacts/p34/`.

B roots: `artifacts/p34-candidate-p41-final/`, `artifacts/p34-candidate-p41-final2/`,
`artifacts/p34-candidate-p41-reconciled/`, `artifacts/p34-candidate-p41ab2/`,
`artifacts/p34-candidate-p41ab3/`,
`artifacts/p34-schema-update-validation-p41-reconciled-final/`,
`artifacts/p34-schema-update-validation-p41-reconciled/`, and `artifacts/p34/`.

Candidate file inventories matched `New-P34Package.ps1`; the A candidate
module/report/archive hashes matched their manifests. Readiness JSON files,
including the alternate ab/v2 names, matched `Invoke-P41UxReadiness.ps1
-OutputPath`. Schema-update output matched `Invoke-P34SchemaUpdateValidation.ps1`.
Gate receipts and coverage files matched the compatibility/coverage tooling.
After `git clean -nd -- artifacts/` preview, each verified root was cleaned
by its exact path. No `-x`, reset, stash, automatic commit of outputs, or
C worktree cleanup was used. A/B status was empty and diff checks passed;
C remained clean. All subsequent external output roots are fresh temp roots.

Validation output hygiene:
candidate/schema-update/reproduction tooling can leave disposable untracked artifacts in persistent worktrees. Future maintenance should prefer disposable temp output roots or explicit cleanup so successful validation does not dirty integration worktrees.

## Semantic and artifact closure

A changed only read-only validation tooling/tests and sanitized documentation.
Its parser, GET-only, candidate binding, and fail-closed tests passed after merge.
No live Cloudflare requests were made for this integration.

B was rebased without conflicts. Its full deterministic gate, P1-P2.4 chain,
compatibility, repeated coverage, admission, CI gate negative tests,
isolated schema-update, candidate smoke, package byte equality, and P4.1
candidate walkthrough passed before merge.

C was rebased last. The only conflicts were the D1 and D2 canonical models.
They were resolved by the fixture/projection/source generators, not by editing
hashes. Relative to B, each model changed only its manifest input hash and
canonical digest. Fixtures, generated source, runtime, P3.2 model, full help,
ETS implementation, and P4.1 tests remained unchanged. Independent read-only
review verified those invariants. C passed static P4.2 security/metadata,
locked restore, and the complete P3.4 deterministic chain before merge.
Final exact-HEAD regression/package/clean-checkout results belong to the receipt.

Pinned schema revision: `28bfb054e5fa106464e9fbbf0ffbf362bc85234d`.
Schema info.version: `4.0.0`.
Schema SHA-256: `f71c82b532b284e41a0e9de41ac1ee398e1b7b658f521e7f8ee478ce922fbf45`.
Schema bootstrap retains `core.autocrlf=false` and `core.eol=lf`.
Remote byte equality remains unverified until a new ABC remote run succeeds.

Formal exports remain exactly `Get-CfZone`, `Get-CfDnsRecord`,
`New-CfDnsRecord`, `Remove-CfDnsRecord`, and `Set-CfDnsRecord`.
D1/D2 remain bounded test-only surfaces. `CfZone.ZoneId` and
`CfDnsRecord.DnsRecordId` remain ETS aliases of `Id`;
records do not acquire `ZoneId`, and record mutation requires explicit parent scope.

## Evidence and release boundaries

P3.5a: Complete (preserved bounded read-only evidence).
P4.1: Complete (preserved design and repeated mock/candidate verification).
P4.2 technical integration: **Complete** for the integrated A/B/C scope. The
overall P4.2 status remains **Partial** because Gallery preflight is **NotReady**
while explicit license information is missing. This summary does not claim
publication or final release acceptance.

P4.2 overall remains Partial; Ready for P4.3: No.
The remaining release-metadata blocker is **custom project license not finalized**.
No LICENSE, LicenseUri, Gallery key, publication, tag, or GitHub Release was added.
Gallery preflight must remain NotReady. Signing remains explicitly deferred
for the first release with the existing recorded risk/revisit policy.

Preserved outside this integration: P3.5b constrained CRUD (Not started;
task documentation prepared and awaiting specific authorization),
P3.5c full transport/rate-limit validation (Partially ready), DNS export
media-type classification (E. still insufficient evidence), formatter/completer
work, Gallery publication, and P4.3. The observed export media type remains
`application/octet-stream; charset=UTF-8` versus pinned `text/plain`; no
runtime, normalizer, or correction change was made to address it.
